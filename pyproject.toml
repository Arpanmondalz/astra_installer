[project]
name = "astra"
version = "2.0.0"
description = "Raspberry Pi telescope camera server for the Astra build"
requires-python = ">=3.9"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-q"
